object thisIsAnIdentifier {
  def main(): Unit = {
  }
}

class ThisISAClass {
  var a: Int[];
  var b: ClassB;

  def meth(d: String): Int[] = {
    var c: Int[];
    c = !a.do().stuff().in().chain();
    return c;
  }
}
